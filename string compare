#include<stdio.h>
#include<string.h>
int main()
{
    char str1[100],str2[100];
    gets(str1);
    gets(str2);
    int x=strlen(str1);
    int y=strlen(str2);
    int i,j,p=0,q=0,r=0;
    if(x!=y)
        printf("string are not equal");
    else
    {
        for(i=0;i<x;i++)
        {
            if(str1[i]==str2[j])
                p++;
            if(str1[i]>str2[j])
                q++;
            if(str1[i]<str2[j])
                r++;
        }
    }
    if(p==x)
     printf("string are equal");
    else if(q>0)
        printf("str1 getter than str2");
    else if(r>0)
        printf("str2 getter than str1");
    return 0;
}
