#include<stdio.h>
#include<string.h>
int main()
{
    char str[100];
    gets(str);
   int i=0;
   while(str[i]!='\0')
   {
       if(str[i]>='a'&&str[i]<='z')
        str[i]=str[i]-32;
       else
        str[i];
       i++;
   }
   str[i]='\0';
   puts(str);
   return 0;
}
