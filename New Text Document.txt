#include<stdio.h>
#include<string.h>
int main()
{
    char source[100],distenation[100];
   printf("enter source string:");
    gets(source);
    printf("enter distenation string:");
    gets(distenation);
   int i=0;
   while(source[i]!='\0')
   {
       i++;
   }
    int j=0;
   while(distenation[j]!='\0')
   {
    source[i]=distenation[j];
       i++;
       j++;
   }
   source[i]='\0';
   puts(source);
   return 0;
}